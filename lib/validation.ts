export function settingsValid(s:any){return s&&Array.isArray(s.stages)&&s.stages.length>=3&&s.stages.length<=8&&new Set(s.stages).size===s.stages.length&&s.stages.every((x:any)=>typeof x==='string'&&x.trim()&&x.length<=35)&&s.stages.includes('Ganho')&&s.stages.includes('Perdido')&&Array.isArray(s.criteria)&&s.criteria.length===4&&s.criteria.every((x:any)=>typeof x==='string'&&x.trim()&&x.length<=100)&&Number.isFinite(s.threshold)&&s.threshold>=25&&s.threshold<=100&&typeof s.followup==='boolean'&&typeof s.qualify==='boolean'}
